# 第2周周末实战任务书：任务管理系统·工程化分层升级版

## 一、任务说明
- 项目目标：在第一周单文件代码基础上，完成工程化分层改造，落地路由模块化、全局异常、中间件、结构化日志四大工程化能力
- 前置基础：已完成第一周单文件CRUD项目，掌握FastAPI基础语法与参数校验
- 存放路径：`01-基础夯实期/02-工程化分层架构/code/`
- 核心要求：业务功能完全兼容无退化，代码分层清晰，职责边界明确，全场景异常有兜底

## 二、核心任务清单
### 1. 目录结构搭建
1. 按照分层规范创建完整目录结构，补充所有`__init__.py`文件
2. 标准目录如下：
app/
├── init.py
├── main.py # 项目主入口，仅负责组装注册
├── routers/ # 路由层
│ ├── init.py
│ └── task.py # 任务模块接口
├── schemas/ # 接口模型层
│ ├── init.py
│ └── task.py # 任务请求 / 响应 Pydantic 模型
└── utils/ # 工具层
├── init.py
├── exceptions.py # 全局异常处理器
└── logger.py # 日志配置
3. 验证目录导入无报错、无循环依赖

### 2. 路由与模型拆分迁移
1. 将所有Pydantic数据模型迁移至`schemas/task.py`，统一导出
2. 将所有任务接口迁移至`routers/task.py`，使用`APIRouter`管理
3. 给路由配置统一前缀`/tasks`、统一文档标签「任务管理」
4. `main.py`仅保留应用初始化与路由注册，不编写任何业务逻辑
5. 内存数据存储逻辑下沉，确保接口功能与拆分前完全一致

### 3. 全局异常体系接入
1. 在`utils/exceptions.py`中实现三类全局异常处理器
2. 统一所有响应错误格式：`{"code": 状态码, "message": "错误描述", "data": null}`
3. 三类异常分别处理：
   - `HTTPException`：透传业务错误信息，包装为统一格式返回
   - `RequestValidationError`：格式化字段错误，返回422，明确展示字段与错误原因
   - `Exception`全局兜底：记录错误日志，返回通用500提示，不暴露堆栈等敏感信息
4. 在`main.py`中注册所有异常处理器

### 4. 请求耗时中间件实现
1. 实现HTTP中间件，基于洋葱模型计算每个请求的处理耗时
2. 在响应头中添加`X-Process-Time`字段，单位为秒
3. 输出结构化访问日志，包含：请求方法、请求路径、响应状态码、处理耗时
4. 确保异常请求也能正常记录日志与返回响应头

### 5. 结构化日志配置
1. 在`utils/logger.py`中封装统一日志配置
2. 统一日志格式：`时间 - 日志级别 - 模块名 - 日志内容`
3. 支持DEBUG / INFO / WARNING / ERROR四级日志分级
4. 替换项目中所有`print`语句，按场景替换为对应级别的日志
5. 所有异常场景必须输出ERROR级日志，附带错误详情

### 6. 全量回归测试与提交
1. 全覆盖正常场景测试，验证所有业务功能与第一周完全一致，无功能退化
2. 全覆盖异常场景测试：参数错误、资源不存在、系统异常
3. 验证响应头、日志输出、错误格式全部符合要求
4. 整理代码，补充关键注释，按规范提交至GitHub对应目录

## 三、验收标准
✅ 目录结构符合分层规范，各层职责边界清晰，无职责混杂
✅ `main.py`无业务代码，仅负责应用初始化与组件注册
✅ 所有接口功能与单文件版完全一致，无功能退化
✅ 三类异常全部统一处理，错误返回格式一致，敏感信息脱敏
✅ 所有接口响应头均包含`X-Process-Time`字段
✅ 终端输出结构化访问日志，格式规范、字段统一
✅ 项目无`print`语句，全部使用logging分级输出
✅ 所有异常场景均有对应ERROR日志，无未捕获的500裸奔错误
✅ 交互式文档中接口按标签分组，路径自动带统一前缀
✅ 代码命名规范，分层清晰，符合PEP8编码规范

## 四、最终项目目录结构
task-manager-architecture/
├── app/
│ ├── init.py
│ ├── main.py
│ ├── routers/
│ │ ├── init.py
│ │ └── task.py
│ ├── schemas/
│ │ ├── init.py
│ │ └── task.py
│ └── utils/
│ ├── init.py
│ ├── exceptions.py
│ └── logger.py
└── README.md

## 五、运行方式
1. 安装依赖：`pip install fastapi uvicorn`
2. 启动服务：`uvicorn app.main:app --reload`
3. 接口文档：http://127.0.0.1:8000/docs